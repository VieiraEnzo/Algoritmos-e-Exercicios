#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main(){
    int n;
    int input;
    vector<int> order;
    cin >>n;

    for(int i=0;i<n;i++){
        cin >> input;
        order.push_back(input);
    }

    
}
