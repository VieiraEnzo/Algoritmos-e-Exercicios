#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fastio  ios_base::sync_with_stdio(false), cin.tie(nullptr)

int main(){
    fastio;
    int v; cin >> v;
    int a,b,c;
    // v = a * b * c
    // S = 2*(a*b+a*c+b*c)
}   //P = (2*(b+c), 2*(a+c), 2*(a+b)) 