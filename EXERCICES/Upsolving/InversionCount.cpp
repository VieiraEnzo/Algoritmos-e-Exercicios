#include <bits/stdc++.h>
typedef long long ll;
using namespace std;


//nao entendi ainda

int main(){
    int t; cin >> t;
    for(int init = 0; init < t; init++){

        int n; cin >> n;
        vector <int> A(n);
        vector<int> Orig(n);
        for(int i =0; i < n; i ++){
            cin >> A[i];
            Orig[i] = A[i];
        }
        sort(A.begin(),A.end());


    }
}
    

