#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> V;
    map<int,int> found;
    int temp;
    int n;
    cin >> n;
    for(int i=0;i < n;i++){
        cin >> temp;
        V.push_back(temp);
    }
    int max =0;
    
}
